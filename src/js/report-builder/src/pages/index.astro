---
import ReportLayout from "@/layouts/ReportLayout.astro";
import PageList from "@/components/report/PageList.astro";
import { getReport } from "@/lib/data";

const report = getReport();
---

<ReportLayout title={`${report.project.title} — InVRT Report`} report={report}>
  <PageList pages={report.pages} />
</ReportLayout>
